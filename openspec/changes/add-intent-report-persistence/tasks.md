## 1. InspectorResultCollector + 闭包注入

- [x] 1.1 实现 per-run `InspectorResultCollector`（`append(result: InspectorResult)` + `snapshot() -> list[InspectorResult]`，**非 module-global**；放 `tools/` 或 `reporting/` 自定）；验收：append/snapshot 单测 + per-run 实例独立
- [x] 1.2 `register_default_tools(registry, *, clock=None, collector=None)`：collector 非 None 时 `run_inspector_handler` append **`InspectorRunner.run` 返回的 `InspectorResult` 对象本身**（带真 status/version/duration），**禁止** append 投影后的 `RunInspectorOutput`（已剥 status/version）；**ok 与非 ok 两分支都 append**（runner 非 ok 路径也构造带 status/version 的 InspectorResult，已确认）；wire 投影 `RunInspectorOutput` 不变；验收：注入后 snapshot 含完整 result（status/version/duration）、非 ok inspector 的 result 也在 snapshot、不注入时行为不变、`run_inspector` tool_result 字节不变（既有 cassette 命中）
- [x] 1.3 `register_diagnostician_tools(registry, *, finding_store, target_name, clock=None, collector=None)`：`request_more_inspection` handler 把补查的 `InspectorResult` append 进同一 collector；验收：补查后 snapshot 含补查 result 单测

## 2. intent 路径 Report 组装（agent-report-assembly）

- [x] 2.1 `cli/_intent.py` 编排改造：装配同一 collector 注入 Planner + 诊断师 registry；Planner → Diagnostician 串接；用 `datetime.now(UTC)` 在 Planner 前/诊断后各取 started_at/finished_at；**诊断 loop 结束后** `collector.snapshot()` → `Report.from_inspector_results(target_name, results, intent=..., started_at=..., finished_at=..., status=<reconcile 覆盖>, token_usage=<两 loop LoopUsage 字段级求和>)`（签名:target_name 在前、started/finished 必填）；验收:补查 result 进 Report.inspector_results 的单测（snapshot 时序）+ started/finished 来自编排层计时
- [x] 2.2 id 统一（**靠 compute_finding_id 内容确定性,非组装顺序**）+ hypotheses/narrative 投影：诊断 loop **前**新增**内联/小 seed-helper**（放 `cli/_intent.py`）——遍历 collector Planner-phase snapshot 的每个 `InspectorResult`，对其 `findings` 经 `compute_finding_id(ir.name, ir.version, f.message)` 盖 id（grouping 由 collector 原生保留,**禁止任何 registry 反查**）→ seed FindingStore（替代被删的 `stamp_planner_findings`）；诊断后 from_inspector_results 用同 version 同函数盖出相等 id；harvest 的 supporting_findings 引用真 id（= Report.findings id）；投影进 `Report.hypotheses`、narrative 进 `Report.metadata["diagnosis_narrative"]`；验收:hypotheses.supporting_findings ⊆ Report.findings id、seed id == Report id（同源）的单测
- [x] 2.3 id 一致性不变量校验：组装后断言每个 `hypotheses[*].supporting_findings` id ∈ `Report.findings` id 集；不满足 fail-loud（→ CLI internal exit 2）；验收:构造不一致 → 断言 raise/exit2 单测
- [x] 2.4 降级/no-result 两路 + status 合并:**判据=collector 是否真空（零 InspectorResult）**。collector 非空(含全非 ok)→ 组装 Report；`meta.status` 经 `from_inspector_results(status=...)`:**reconcile 产 `degraded_*`/`empty_response` → 传该值(覆盖;`_derive_report_status` 产不出 empty_response,漏传会丢失);reconcile=ok → 传 `status=None`**(让 `_derive_report_status` 按 §9 推 —— 全 ok→ok / 非 ok 仅 timeout 且有 ok→仍 ok / unreachable·exception·requires_unmet 或全 timeout→partial;**禁止显式传 ok、禁止 blanket「有非 ok 就 partial」**);collector 真空→ 不产 Report、no-result 路径（**判据是 collector 真空,不止 failed_api_unavailable**——也含「Planner ok 但模型从不调 run_inspector」致 snapshot 空;`from_inspector_results` 对空列表会 raise ValueError,故须在 snapshot 后先判空走 no-result;**no-result 的 stderr 降级文案勿硬编码 `failed_api_unavailable`**,泛化为「无可持久化结果」类）;验收:三类参数化单测(降级覆盖 / 全非 ok→partial / 真空 no-result,含 Planner-ok-但零-inspector 子case)

## 3. CLI 渲染 / 退出码 / 持久化

- [x] 3.1 `--intent` 渲染：json 走 `reporting/render_json`(脱敏);md **新增 intent 风格 Report 渲染器**(对脱敏后 Report)= narrative(从 `metadata["diagnosis_narrative"]`,容忍空、空不渲染空标题) + `## Findings`(Report.findings) + `## 根因假设`(空→占位) + 遥测;**禁止**让 `--intent` md 走 `reporting/render_markdown` 全量结构(meta 表 + `## Inspector Results` 原始 JSON dump);**不改** `reporting/render_markdown` / `render_planner_result`(demo/`--inspector` 用);验收:md snapshot(含根因假设 + narrative,无 `## Inspector Results` JSON dump,`.rstrip("\n")` 容忍尾换行)、空假设占位、空 narrative 不渲染空标题、demo md 不回归
- [x] 3.2 `cli/inspect.py` `--intent` 退出码改由 **Report** 映射（新增 Report 版退出码函数；0/1/2/3，critical 基于 Report.findings，降级 status(`degraded_*`/`empty_response`/**`partial`**)→2，no-result→2）；**注意须含 `partial`**(相对 `_compute_diag_exit_code` 的新增行为,DiagnosticianResult.status 永不为 partial 但 Report.meta.status 可);**不改** `_compute_intent_exit_code(PlannerResult)`(demo 共享);更新 `--persist` help 文案 + `inspect_cmd` docstring 退出码注释(反映 intent 现产 Report);`--inspector` 零改动;验收:健康/critical/降级/**partial**(inspector 非 ok 推导,区别于 loop 降级)/no-result 退出码 CliRunner 测试 + 断言 demo run 退出码/渲染不回归
- [x] 3.3 解锁 `--intent --persist`:移除 `--persist` 对 `--intent` 的拒绝；`--intent` 产 Report 后调 `ReportStore.save`；no-result（无 Report）时显式跳过 persist（不静默假成功）；demo run 仍拒绝 --persist；验收:`--intent --persist` 入库 + `reports show` 取回含 hypotheses 的 Report + no-result 不入库 的 CliRunner 测试
- [x] 3.4 `--intent --format json` BREAKING:输出 Report（非 DiagnosticianResult），可 `Report.model_validate_json` 往返；验收:往返解析 + 含 meta/hypotheses/metadata[diagnosis_narrative] 的 snapshot

## 4. 端到端 + 回归 + 收尾

- [x] 4.1 端到端 `tests/cli/test_inspect_intent_persist.py`(沿用 test_inspect_intent.py monkeypatch create_backend + authored FakeBackend pattern):`--intent --persist` 产忠实 Report（meta.status/inspectors_used/token_usage + hypotheses 该 fixture 下非空）入库；`reports show` 取回；同 target 两次 `reports diff` 输出 finding 级 added/resolved（**两次 run 的 authored 脚本都须终结于 status=ok**,否则 `compute_diff` 命中 `baseline_not_ok` skip 致断言失败）；含补查并引用新 finding 的用例；验收:CliRunner + 无真实 API、可重现
- [x] 4.2 脱敏回归:`--intent` 的 md/json/stderr 均经脱敏（Report 渲染走 redact_report_for_render；stderr 进度沿用 diagnostician 的 redact_text）；构造含 secret 的 finding/narrative 断言掩码；验收:secret 不现于 stdout/json/stderr
- [x] 4.3 既有测试迁移 + 死代码清理:`tests/cli/test_inspect_intent_diagnostician.py` 中断言 DiagnosticianResult-as-CLI-output 的用例迁到 Report（json 往返、退出码、根因章节）;**删除 `stamp_planner_findings` 函数 + 其全部单测**——含 `tests/tools/test_diagnostician_tools.py` 中相关用例**与 `tests/cli/test_inspect_intent_diagnostician.py::test_intent_stamping_inspector_unloaded_internal_exit_2`**(它 monkeypatch `stamp_planner_findings`;其保护的 registry-反查-not_found 场景不再可能——**该测试是删除而非迁移**,其 CLI `internal:→exit 2` 边界覆盖由 task 2.3 新增的「id 不变量失败 → internal exit 2」用例替代承接);**顺带 scrub 残留 docstring**:`_intent.py`/`inspect.py` 中提及已删 `stamp_planner_findings` 的注释/docstring(如 `_intent.py:283`「re-looks-up versions」、`inspect.py:634`「bubbled by stamp_planner_findings」)改为新 seed-helper 描述,勿留死名;确认 `--inspector` + demo 全套不回归(demo 不经 Report/intent 渲染器/collector,零影响)
- [x] 4.4 `mypy --strict` 全绿、`ruff` 全绿；更新 `TODO.md`（M3.1 持久化半边完成、M3 退出条件持久化部分达成；标注 demo wiring + hypothesis-diff 为后续）；`openspec-cn validate add-intent-report-persistence` 通过；全量 `pytest` 无回归
